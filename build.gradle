apply plugin: "eclipse"
apply plugin: 'groovy'

repositories {
  mavenCentral()
}

dependencies {
    compile gradleApi()
    compile "org.mybatis:mybatis:3.0.6" 
    groovy localGroovy()
}

task wrapper(type:Wrapper) {
  gradleVersion = '1.0'
}
